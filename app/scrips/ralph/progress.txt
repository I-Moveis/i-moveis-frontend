# Ralph Progress Log
Started: Mon May 11 08:28:57 PM -03 2026

## Codebase Patterns
- Project is a Flutter app; primary working dir is `app/`. Quality gate is `flutter analyze` + `flutter pub get`. Build target for this branch: Android only.
- API base URL is centralized in `lib/core/constants.dart` via `kApiBaseUrl` (compile-time `String.fromEnvironment('API_BASE_URL')`). Override at build time with `--dart-define=API_BASE_URL=...`.
- `absoluteImageUrl(...)` in `lib/core/constants.dart` historically force-upgraded `http://` to `https://`; on `hospedagem_local` we strip that to support localhost serving over plaintext.
- Android cleartext traffic is already enabled in `android/app/src/main/AndroidManifest.xml` (`android:usesCleartextTraffic="true"`).
- Branch policy: `development` is sacrosanct on this work — never moved or rewritten. All hospedagem_local work branches off it.

---
## 2026-05-11 - US-001
- Verified branch `hospedagem_local` exists and is checked out (was already created from `development`).
- Confirmed `development` and `hospedagem_local` both point at `a0ae284` (Merge PR #56). development bit-for-bit identical to its tip before this story; no commits added on development.
- `git branch --show-current` => `hospedagem_local`.
- `git status` clean on development (only untracked files in working tree, scoped to scrips/ tasks/ test/provider_init_test.dart — none touch development).
- Files changed: `scrips/ralph/progress.txt` (this log), `scrips/ralph/prd.json` (US-001 passes -> true).
- **Learnings for future iterations:**
  - Branch was already created before Ralph started; the work for US-001 was verification, not creation. When a story is purely "ensure X state", the deliverable is documentation in progress.txt + PRD flip.
  - `git rev-parse <ref>` is the cheapest way to assert two refs are at the same SHA without diff noise.
  - `git log --all --source --decorate` is useful to confirm a branch tracks the expected upstream.
  - Untracked files (scrips/, tasks/, test/provider_init_test.dart) are present at session start; these belong to ralph scaffolding and the spec doc — they are not contamination from development.
---
## 2026-05-11 - US-002
- Edited `lib/core/constants.dart`: default fallback de `kApiBaseUrl` mudou de `https://lab.alphaedtech.org.br/server01/api/` para `http://localhost:3000/api/`. Override via `--dart-define=API_BASE_URL=...` continua funcionando.
- Atualizei a docstring acima do getter para refletir o novo default e mencionar como apontar de volta ao remoto via dart-define.
- `grep -rn "lab.alphaedtech" lib/` retorna 0 ocorrencias (validado).
- `flutter analyze` => 54 issues pre-existentes (nenhum em `lib/core/constants.dart`); zero novos warnings introduzidos.
- Diff escopo: apenas `lib/core/constants.dart` (5 inserts / 3 deletes). PRD e progress.txt sao deltas de tracking, fora do escopo de codigo.
- Files changed: `lib/core/constants.dart`, `scrips/ralph/prd.json` (US-002 passes -> true), `scrips/ralph/progress.txt` (este log).
- **Learnings for future iterations:**
  - O criterio `grep "lab.alphaedtech" lib/` retornar 0 e ESTRITO — nao basta trocar o `return`, qualquer mencao ao dominio (incluindo em docstrings/exemplos) precisa sumir do `lib/`. Mantive a referencia generica ("apontar de volta para o backend remoto") sem citar URL.
  - `flutter analyze` na linha base ja tem 54 issues `info`-level em outros arquivos. Para "sem warnings novos" basta verificar que nenhum do baseline pertence ao arquivo editado e nenhum issue novo foi introduzido. Fazer grep por nome do arquivo na saida e o jeito mais rapido.
  - A constante e referenciada via `kApiBaseUrl` (getter) pois o valor depende de `String.fromEnvironment` que precisa de avaliacao em call-site. Nao tente convertir em `const` top-level.
---
